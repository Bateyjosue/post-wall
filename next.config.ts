import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "npxojulztomtxnibjoff.supabase.co", // <-- your actual Supabase project domain
    ],
  },
  /* config options here */
};

export default nextConfig;
